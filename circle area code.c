#include<stdio.h>
int main(){
  // finding circle area 
  float radius,area ;
  printf("enter the circle radius:\t");
  scanf("%f",&radius);
  area = 3.14*radius*radius;
  printf("the area of circle is %f",area );
  
  return 0;
}
